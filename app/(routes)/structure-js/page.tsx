const StructureJSPage = () => {

    // ----------- بزرگ ترین عضو آرایه -----------

    // const findMax = (arr: Array<number>) => {
    //     return Math.max(...arr)
    // }

    // const arr = [1, 2, 10, 0, 5, 85, 8]

    // console.log(findMax(arr));

    // --------- رشته برعکس یکی باشه  -----------

    // const isPalindrom = (str: string) => {
    //     let tempStr = str.split("").reverse().join("")

    //     return tempStr === str
    // }

    // console.log(isPalindrom("level"))

    // ----------- حذف تکراری -----------

    // const removeDup = (arr: any[]) => {
    //     return [...new Set(arr)]
    // }

    // console.log(removeDup([1, 2, 4, 6, 6]))

    return (<></>);
}

export default StructureJSPage;